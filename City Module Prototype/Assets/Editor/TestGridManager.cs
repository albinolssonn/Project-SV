using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using NUnit.Framework;

public class TestGridManager
{
    /*
    [Test]
    public void GetNearbyCells_Test()
    {
        GridManager grid = new GridManager();
        grid.Start();

        int rows = grid.GetRows();
        int cols = grid.GetCols();

        Assert.IsTrue(rows > 2);
        Assert.IsTrue(cols > 2);

        ArrayList output1 = grid.GetNearbyCells(0, 0);
        Assert.AreEqual(output1.Count, 3);

    }
    */

}
